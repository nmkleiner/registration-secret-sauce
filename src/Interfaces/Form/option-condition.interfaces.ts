export interface OptionCondition {
  optionId: string;
  fieldId: string;
  triggeredFieldId: string;
}

export interface SectionOptionCondition {
  optionId: string;
  fieldId: string;
  triggeredSectionId: string;
}
